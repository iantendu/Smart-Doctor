<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartdoctor">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_baseline_medical_services_24"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartDoctor">
        <activity android:name=".MessagePatient"></activity>
        <activity android:name=".PatientProfileActivity" />
        <activity android:name=".DoctorProfile" />
        <activity android:name=".MessageDoctor" />
        <activity android:name=".DoctorHome" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".DoctorRegister" />
        <activity android:name=".DoctorLogin" />
        <activity android:name=".PatientRegister" />
        <activity android:name=".PatientLogin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>